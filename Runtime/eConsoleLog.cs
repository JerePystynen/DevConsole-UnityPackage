namespace Exodia
{
    public enum eConsoleLog {
        //everything typed in + output
        everything,
        //validated typed command + output
        validated,
        //only output
        output,
    }
}